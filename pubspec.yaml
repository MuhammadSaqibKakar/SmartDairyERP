name: smart_dairy_erp
description: Smart Dairy ERP app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  intl: ^0.20.2       # Updated to latest compatible version
  fl_chart: ^1.1.1    # Updated to latest stable version compatible with Flutter 3.10+

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/baby_cow.png
